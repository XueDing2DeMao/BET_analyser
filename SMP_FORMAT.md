# SMP 原生读取范围与验证

本实现是受限的格式适配器，不是 Micromeritics 官方 SDK。真实验证限于一个 ASAP 2460 Version 3.01 的 SMP/XLS 成对样品。测试仓库只保存自行构造的二进制格式与公开合成等温线，不保存用户原始文件。

## 二进制结构

- 文件签名 `^vMIC##&&FS\0`，容器版本 `0200`，第 24 字节起存放目录地址与分配长度。
- 目录为 SUBSET101，版本 2，包含 `uint16` 数目和 `uint16 id / uint32 address / uint32 payload_length` 目录项。按目录地址定位，不依赖样例的绝对偏移、样品文件名或测量值。目录边界、子集标识/长度、重复和重叠都需一致。
- SUBSET301 v19 保存变长 UTF-16LE 样品信息与 double 样品质量。字符串通过标签与字节长度读取。
- SUBSET303 v13 保存仪器版本、测量记录、分支点数、逐点饱和蒸气压、温度及自由空间参数。记录长 66 字节，包含绝对压力、相对压力、累计投气量、时间及上一点压力。首条零记录不参与分析。
- SUBSET337 v15 用于确认氮气、分子横截面积、液态体积换算系数及非理想气体校正因子与计算模型相符。
- 用文件中的吸附点数分支，转折点由两支共用，不通过吸附量增减猜测分支。原始压力和相对压力还需与同时间的饱和蒸气压相符。

这些字段定义来自本地二进制对照，尚无厂家公开二进制规范背书。不会对未知字段配置猜测默认值。样品管配置采用已验证结构的严格比较，因此某些有效的其他 SMP 文件也会被拒绝，仍可通过 XLS 导入。

## 吸附量校正

厂家给出了累计投气量减去自由空间气体量、再按样品质量归一化的计算框架，以及冷区体积的计算方式。参见 [Calculations - 2460 ASAP, Dec 2024](https://downloads.micromeritics.com/Calculations/Calculations-2460-ASAP-Dec-2024.pdf)，43–45 页。PDF 中部分公式排版存在歧义，应结合气体状态方程的量纲核对。

在已验证配置中，从 SMP 读取两项附加校正系数 f₀ 和 f₁。还原计算为：

```text
V_bath = (V_cold − V_warm) / (1 − T_bath / T_ambient)
V_effective = V_cold − f₀ × V_bath
n_free = (P / 760 mmHg) × [V_effective + P × α × V_bath × (1 − f₁)]
q = (n_dosed − n_free) / mass
```

压力单位为 mmHg，质量单位为 g，结果为 cm³(STP)/g。f₀/f₁ 字段的解释通过成对样品核验得到，不能声称为官方公布规范。程序读取文件内的系数，不使用 XLS 的回归参数或硬编码真实样品值。

成对验证共 62 次测量，形成 35 个吸附点和 28 个脱附点（共用转折点）。相对压力逐值相同；吸附量最大绝对差 1.0213×10⁻⁵ cm³(STP)/g，最大相对差 1.4888×10⁻⁶；脱附支最大绝对差约 3.6×10⁻¹⁴ cm³(STP)/g。不能由一个样品的数值一致推断其他版本与配置均兼容。

## 分析与报告

SMP 只提供本实现已核验的原始等温线和测量参数，未读取用户后来编辑的仪器报告选区/校正设置，也未解码仪器的 BJH 报告表。输出明确为本项目计算值；BET 优先选择满足 Rouquerol 判据的区间，否则保留普通等温线入口的默认区间与适用性提示。需要仪器报告对照和仪器 BJH 分布表时使用其导出的 XLS/XLSX。

回归覆盖变长样品名、不同质量、移动的数据块、变化的校正系数、损坏的目录/记录、截断文件、气体和物性不符、未支持版本/配置，以及无需第二个文件的网页上传。
